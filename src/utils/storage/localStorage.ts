const localStorage = {
  get: (key?: string) => {},
  set: (key: string, value: any) => {},
  remove: (key: string) => {},
  reset: () => {},
  has: (key: string) => {},
  exist: () => {},
};

export default localStorage;
